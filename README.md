# healenium-test
Selenium + Healenium demo in Java that auto-heals broken locators on a web form — filling inputs, dropdowns, checkboxes, radio buttons, and date pickers using TestNG and WebDriverManager.
